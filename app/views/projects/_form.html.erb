<%= simple_form_for(@project) do |f| %>
  <%= f.input :name %>
  <%= f.input :description, hint: "What does your project do?" %>
  <%= f.input :url, hint: "Link to the project (if there is one). Ensure full url ie. https://" %>
  <%= f.submit %>
<% end %>
